# CS306_Part2

To access pre-processed images, make sure to `import joblib`.

Run `x_train, y_train = joblib.load('train_data.joblib')` to load training data

Run `x_test, y_test = joblib.load('test_data.joblib')` to load testing data